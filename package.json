{
  "name": "tiktok-real-sign-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "console": "node console-app.js",
    "build": "npm install && npx playwright install --with-deps chromium",
    "postinstall": "npx playwright install --with-deps chromium || true"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pino": "^8.15.0",
    "rate-limiter-flexible": "^2.4.1",
    "dotenv": "^16.3.1",
    "tiktok-signature": "github:carcabot/tiktok-signature",
    "tiktok-live-connector": "^2.0.8",
    "playwright": "^1.40.0"
  },
  "engines": {
    "node": ">=18"
  }
}
